<?xml version="1.0" encoding="UTF-8"?>
<!--
  Settings Maven usado SOMENTE no CI.
  - Resolve as bibliotecas internas (com.vmetrix) do GitHub Packages deste repositório.
  - Credenciais vêm de variáveis de ambiente injetadas pelo Actions (GITHUB_TOKEN efêmero);
    nenhuma credencial fica versionada.
  - Os poms das aplicações não foram alterados: repositório e destino de deploy
    são definidos aqui e via -DaltDeploymentRepository.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">
  <servers>
    <server>
      <id>github</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>

  <profiles>
    <profile>
      <id>github-packages</id>
      <repositories>
        <repository>
          <id>central</id>
          <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <repository>
          <id>github</id>
          <url>https://maven.pkg.github.com/${env.GITHUB_REPOSITORY}</url>
          <releases><enabled>true</enabled></releases>
          <snapshots><enabled>false</enabled></snapshots>
        </repository>
      </repositories>
    </profile>
  </profiles>

  <activeProfiles>
    <activeProfile>github-packages</activeProfile>
  </activeProfiles>
</settings>
